const helper = require("./helper");
// alternative for above import, but this only imports single function form helper.js flie //const {sum} = require("./helper");
//var total =sum(20,30)
var total = sum(20, 30);
// var products = helper.product(12, 3);

console.log("Total : " + total);
// console.log("Total : " + products);